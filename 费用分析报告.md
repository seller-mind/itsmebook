# 阿里云百炼API费用分析报告

## 一、费用去向分析

### 1.1 定价确认（阿里云百炼官方2026年5月）

| 模型 | 分辨率 | 单价 | 说明 |
|------|--------|------|------|
| wan2.7-image | 1K/2K | **0.2元/张** | 基础版，速度快 |
| wan2.7-image-pro | 1K | **0.5元/张** | 专业版，支持4K |
| wan2.7-image-pro | 2K | **0.5元/张** | 专业版 |
| wan2.7-image-pro | 4K | **0.5元/张** | 专业版 |

> ⚠️ **关键发现**：代码中使用的是 `wan2.7-image-pro`（Pro版），单价比普通版贵 **2.5倍**！

### 1.2 当前代码配置（app/create/page.tsx）

```typescript
// 第325行
model: "wan2.7-image-pro",  // ❌ 使用Pro版，0.5元/张

// 第329行  
parameters: { 
  size: "1024*1024",  // 1K分辨率
  n: 1  // ✅ 每次生成1张，没有浪费
}
```

### 1.3 理论成本计算

| 场景 | 模型 | 单价 | 20页绘本成本 |
|------|------|------|-------------|
| 当前配置 | wan2.7-image-pro | 0.5元 | **10元/本** |
| 优化后 | wan2.7-image | 0.2元 | **4元/本** |
| 节省 | - | - | **6元/本 (60%)** |

### 1.4 55元花费去向推测

**场景A：使用Pro版**
- 55元 ÷ 0.5元 = 110张图 = **5.5本绘本**

**场景B：普通版**
- 55元 ÷ 0.2元 = 275张图 = **13.75本绘本**

**可能浪费情况**：
- 5本绘本 + 每次超时重试1次 = 5×20×1.5×0.5 = 75元 ❌
- 如果有重试，实际调用次数 > 页面数

---

## 二、浪费点排查

### 2.1 ✅ 已排除的浪费

| 检查项 | 状态 | 说明 |
|--------|------|------|
| n参数 > 1 | ✅ 无问题 | 代码设 `n: 1`，每次只生成1张 |
| 并发数过大 | ✅ 可接受 | batchSize=5，合理 |
| 图片尺寸过大 | ⚠️ 可优化 | 1024×1024对绘本偏高 |

### 2.2 ❌ 主要浪费点

#### **浪费点1：使用Pro版而非普通版（最大浪费）**
```typescript
// 当前代码
model: "wan2.7-image-pro",  // 0.5元/张

// 建议改为
model: "wan2.7-image",      // 0.2元/张
```
**影响**：每张图多花 0.3元，20张绘本多花 **6元/本**

#### **浪费点2：超时重试机制**
```typescript
// 第300-356行
for (let attempt = 0; attempt < 2; attempt++) {  // 最多重试2次
  // ...
  // 只有超时错误才重试
  if (!lastError?.message.includes('超时')) {
    break;
  }
}
```
**分析**：
- 只有超时错误才重试
- 最多尝试2次，即每张图最多调用3次API（1次+2次重试）
- 如果超时频繁，可能产生额外费用

#### **浪费点3：分辨率过高**
```typescript
size: "1024*1024"  // 1K分辨率
```
**建议**：绘本实际展示时会缩放，可考虑更小的分辨率

---

## 三、降本方案

### 方案1：切换到普通版（推荐，节省60%）

```typescript
// app/create/page.tsx 第325行修改

const wanRes = await fetchWithTimeout(
  'https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation',
  {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${dashscopeKey}`,
    },
    body: JSON.stringify({
      model: "wan2.7-image",  // ✅ 改为普通版：0.2元/张
      input: {
        messages: [{ role: "user", content: requestContent }]
      },
      parameters: { size: "1024*1024", n: 1 }
    }),
  },
  45000
);
```

**效果**：
- 20页绘本：10元 → 4元
- 节省 **60%** 费用
- 普通版对绘本场景完全够用

---

### 方案2：添加并发限制（防止滥用）

```typescript
// 添加全局计数器
let concurrentCount = 0;
const MAX_CONCURRENT = 5;  // 最大并发数

// 在generateSingleImage中添加
const waitForSlot = async () => {
  while (concurrentCount >= MAX_CONCURRENT) {
    await new Promise(resolve => setTimeout(resolve, 1000));
  }
  concurrentCount++;
};

// 函数结束时
concurrentCount--;
```

---

### 方案3：降低分辨率（可选）

```typescript
parameters: { 
  size: "768*768",  // ✅ 更小分辨率，成本更低
  n: 1 
}
```

**注意**：分辨率降低可能影响打印质量，建议保留选项让用户选择

---

### 方案4：添加费用估算提示

```typescript
// 在生成前显示预估费用
const ESTIMATED_COST = {
  'wan2.7-image-pro': 0.5,
  'wan2.7-image': 0.2
};

const estimatedFee = ESTIMATED_COST['wan2.7-image'] * 20; // 假设20页
console.log(`预估费用: ${estimatedFee}元`);

// 或者显示给用户
setGenerationStatus(`预估费用约${estimatedFee}元，开始生成...`);
```

---

### 方案5：限制重试次数

```typescript
// 当前：最多重试2次
for (let attempt = 0; attempt < 2; attempt++) {

// 建议：只重试1次或不重试
for (let attempt = 0; attempt < 1; attempt++) {  // 只重试1次
```

---

## 四、最佳实践建议

### 4.1 立即可实施的优化

| 优化项 | 预期节省 | 风险 |
|--------|---------|------|
| 切换到普通版 | 60% | 无，绘本质量足够 |
| 减少重试次数 | 10-20% | 低，可能影响失败率 |
| 添加费用预估 | - | 无，纯体验优化 |

### 4.2 推荐配置

```typescript
const IMAGE_CONFIG = {
  model: "wan2.7-image",       // ✅ 普通版
  size: "1024*1024",           // 1K分辨率
  n: 1,                        // 每次1张
  maxRetries: 1,              // 最多重试1次
  timeout: 45000               // 45秒超时
};
```

---

## 五、成本对比总结

### 每月100本绘本的年化成本

| 配置 | 单本成本 | 100本/月 | 一年 |
|------|---------|---------|------|
| 当前配置 (Pro版) | 10元 | 1000元 | **12000元** |
| 优化后 (普通版) | 4元 | 400元 | **4800元** |
| **节省** | 6元 | 600元 | **7200元/年** |

---

## 六、行动清单

### 高优先级（立即实施）
- [ ] 将 `wan2.7-image-pro` 改为 `wan2.7-image`

### 中优先级（本周内实施）
- [ ] 将重试次数从2次改为1次
- [ ] 添加费用预估功能

### 低优先级（可选）
- [ ] 提供分辨率选项让用户选择
- [ ] 添加并发控制防滥用

---

## 附录：API响应中的费用信息

每次API调用后，可从响应中获取实际费用：

```typescript
if (wanRes.ok) {
  const wanData = await wanRes.json();
  const imageUrl = wanData.output?.choices?.[0]?.message?.content?.[0]?.image;
  const usage = wanData.usage;
  console.log(`生成成功: ${usage.image_count}张, 分辨率: ${usage.size}`);
  return imageUrl;
}
```

建议记录每次调用的 `usage` 信息，用于后续费用审计。
